/*
 * SnapTrade
 * Connect brokerage accounts to your app for live positions and trading
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: api@snaptrade.com
 *
 * NOTE: This class is auto generated by Konfig (https://konfigthis.com).
 * Do not edit the class manually.
 */


package com.snaptrade.client.model;

import java.util.Objects;
import java.util.Arrays;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import java.io.IOException;
import java.time.LocalDate;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import org.apache.commons.lang3.StringUtils;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Map.Entry;
import java.util.Set;

import com.snaptrade.client.JSON;

/**
 * A user&#39;s AUM placement within a single SnapTrade customer&#39;s book
 */
@ApiModel(description = "A user's AUM placement within a single SnapTrade customer's book")@javax.annotation.Generated(value = "Generated by https://konfigthis.com")
public class UserAumPercentileObjectNullable {
  /**
   * The band the user falls into. Deliberately coarse: the underlying totals are only as current as each brokerage&#39;s last sync, so an exact percentile would imply more precision than the data supports. 
   */
  @JsonAdapter(BucketEnum.Adapter.class)
 public enum BucketEnum {
    TOP_1_PERCENT("TOP_1_PERCENT"),
    
    TOP_5_PERCENT("TOP_5_PERCENT"),
    
    TOP_10_PERCENT("TOP_10_PERCENT"),
    
    TOP_25_PERCENT("TOP_25_PERCENT"),
    
    TOP_50_PERCENT("TOP_50_PERCENT"),
    
    BOTTOM_50_PERCENT("BOTTOM_50_PERCENT");

    private String value;

    BucketEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static BucketEnum fromValue(String value) {
      for (BucketEnum b : BucketEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      throw new IllegalArgumentException("Unexpected value '" + value + "'");
    }

    public static class Adapter extends TypeAdapter<BucketEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final BucketEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public BucketEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return BucketEnum.fromValue(value);
      }
    }
  }

  public static final String SERIALIZED_NAME_BUCKET = "bucket";
  @SerializedName(SERIALIZED_NAME_BUCKET)
  private BucketEnum bucket;

  public static final String SERIALIZED_NAME_PERCENTILE = "percentile";
  @SerializedName(SERIALIZED_NAME_PERCENTILE)
  private Integer percentile;

  public static final String SERIALIZED_NAME_COHORT_SIZE = "cohort_size";
  @SerializedName(SERIALIZED_NAME_COHORT_SIZE)
  private Integer cohortSize;

  public static final String SERIALIZED_NAME_AS_OF = "as_of";
  @SerializedName(SERIALIZED_NAME_AS_OF)
  private LocalDate asOf;

  public static final String SERIALIZED_NAME_CURRENCY = "currency";
  @SerializedName(SERIALIZED_NAME_CURRENCY)
  private String currency;

  public UserAumPercentileObjectNullable() {
  }

  public UserAumPercentileObjectNullable bucket(BucketEnum bucket) {
    
    
    
    
    this.bucket = bucket;
    return this;
  }

   /**
   * The band the user falls into. Deliberately coarse: the underlying totals are only as current as each brokerage&#39;s last sync, so an exact percentile would imply more precision than the data supports. 
   * @return bucket
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(example = "TOP_25_PERCENT", value = "The band the user falls into. Deliberately coarse: the underlying totals are only as current as each brokerage's last sync, so an exact percentile would imply more precision than the data supports. ")

  public BucketEnum getBucket() {
    return bucket;
  }


  public void setBucket(BucketEnum bucket) {
    
    
    
    this.bucket = bucket;
  }


  public UserAumPercentileObjectNullable percentile(Integer percentile) {
    if (percentile != null && percentile < 0) {
      throw new IllegalArgumentException("Invalid value for percentile. Must be greater than or equal to 0.");
    }
    if (percentile != null && percentile > 100) {
      throw new IllegalArgumentException("Invalid value for percentile. Must be less than or equal to 100.");
    }
    
    
    this.percentile = percentile;
    return this;
  }

   /**
   * The percent of the cohort the user&#39;s assets are strictly above, 0-100. Integer by design: the distribution is stored as 101 interpolated cutoffs, so a fractional percentile would not mean anything.  Prefer &#x60;bucket&#x60; for anything you display prominently. The distribution is recomputed monthly, so a user&#39;s percentile can move a few points on its own as other users&#39; holdings refresh, while their bucket stays put. Users tied on the same total all receive the lowest percentile that total spans. 
   * minimum: 0
   * maximum: 100
   * @return percentile
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(example = "78", value = "The percent of the cohort the user's assets are strictly above, 0-100. Integer by design: the distribution is stored as 101 interpolated cutoffs, so a fractional percentile would not mean anything.  Prefer `bucket` for anything you display prominently. The distribution is recomputed monthly, so a user's percentile can move a few points on its own as other users' holdings refresh, while their bucket stays put. Users tied on the same total all receive the lowest percentile that total spans. ")

  public Integer getPercentile() {
    return percentile;
  }


  public void setPercentile(Integer percentile) {
    if (percentile != null && percentile < 0) {
      throw new IllegalArgumentException("Invalid value for percentile. Must be greater than or equal to 0.");
    }
    if (percentile != null && percentile > 100) {
      throw new IllegalArgumentException("Invalid value for percentile. Must be less than or equal to 100.");
    }
    
    this.percentile = percentile;
  }


  public UserAumPercentileObjectNullable cohortSize(Integer cohortSize) {
    
    
    
    
    this.cohortSize = cohortSize;
    return this;
  }

   /**
   * Number of your users the distribution was computed from.
   * @return cohortSize
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(example = "24193", value = "Number of your users the distribution was computed from.")

  public Integer getCohortSize() {
    return cohortSize;
  }


  public void setCohortSize(Integer cohortSize) {
    
    
    
    this.cohortSize = cohortSize;
  }


  public UserAumPercentileObjectNullable asOf(LocalDate asOf) {
    
    
    
    
    this.asOf = asOf;
    return this;
  }

   /**
   * The month whose distribution produced this placement.
   * @return asOf
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(example = "Sat Aug 01 00:00:00 UTC 2026", value = "The month whose distribution produced this placement.")

  public LocalDate getAsOf() {
    return asOf;
  }


  public void setAsOf(LocalDate asOf) {
    
    
    
    this.asOf = asOf;
  }


  public UserAumPercentileObjectNullable currency(String currency) {
    
    
    
    
    this.currency = currency;
    return this;
  }

   /**
   * The currency the distribution was computed in.
   * @return currency
  **/
  @javax.annotation.Nullable
  @ApiModelProperty(example = "USD", value = "The currency the distribution was computed in.")

  public String getCurrency() {
    return currency;
  }


  public void setCurrency(String currency) {
    
    
    
    this.currency = currency;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the UserAumPercentileObjectNullable instance itself
   */
  public UserAumPercentileObjectNullable putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    UserAumPercentileObjectNullable userAumPercentileObjectNullable = (UserAumPercentileObjectNullable) o;
    return Objects.equals(this.bucket, userAumPercentileObjectNullable.bucket) &&
        Objects.equals(this.percentile, userAumPercentileObjectNullable.percentile) &&
        Objects.equals(this.cohortSize, userAumPercentileObjectNullable.cohortSize) &&
        Objects.equals(this.asOf, userAumPercentileObjectNullable.asOf) &&
        Objects.equals(this.currency, userAumPercentileObjectNullable.currency)&&
        Objects.equals(this.additionalProperties, userAumPercentileObjectNullable.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(bucket, percentile, cohortSize, asOf, currency, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class UserAumPercentileObjectNullable {\n");
    sb.append("    bucket: ").append(toIndentedString(bucket)).append("\n");
    sb.append("    percentile: ").append(toIndentedString(percentile)).append("\n");
    sb.append("    cohortSize: ").append(toIndentedString(cohortSize)).append("\n");
    sb.append("    asOf: ").append(toIndentedString(asOf)).append("\n");
    sb.append("    currency: ").append(toIndentedString(currency)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("bucket");
    openapiFields.add("percentile");
    openapiFields.add("cohort_size");
    openapiFields.add("as_of");
    openapiFields.add("currency");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

 /**
  * Validates the JSON Object and throws an exception if issues found
  *
  * @param jsonObj JSON Object
  * @throws IOException if the JSON Object is invalid with respect to UserAumPercentileObjectNullable
  */
  public static void validateJsonObject(JsonObject jsonObj) throws IOException {
      if (jsonObj == null) {
        if (!UserAumPercentileObjectNullable.openapiRequiredFields.isEmpty()) { // has required fields but JSON object is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in UserAumPercentileObjectNullable is not found in the empty JSON string", UserAumPercentileObjectNullable.openapiRequiredFields.toString()));
        }
      }
      if ((jsonObj.get("bucket") != null && !jsonObj.get("bucket").isJsonNull()) && !jsonObj.get("bucket").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `bucket` to be a primitive type in the JSON string but got `%s`", jsonObj.get("bucket").toString()));
      }
      if ((jsonObj.get("currency") != null && !jsonObj.get("currency").isJsonNull()) && !jsonObj.get("currency").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `currency` to be a primitive type in the JSON string but got `%s`", jsonObj.get("currency").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!UserAumPercentileObjectNullable.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'UserAumPercentileObjectNullable' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<UserAumPercentileObjectNullable> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(UserAumPercentileObjectNullable.class));

       return (TypeAdapter<T>) new TypeAdapter<UserAumPercentileObjectNullable>() {
           @Override
           public void write(JsonWriter out, UserAumPercentileObjectNullable value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additonal properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else if (entry.getValue() == null) {
                   obj.addProperty(entry.getKey(), (String) null);
                 } else {
                   obj.add(entry.getKey(), gson.toJsonTree(entry.getValue()).getAsJsonObject());
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public UserAumPercentileObjectNullable read(JsonReader in) throws IOException {
             JsonObject jsonObj = elementAdapter.read(in).getAsJsonObject();
             validateJsonObject(jsonObj);
             // store additional fields in the deserialized instance
             UserAumPercentileObjectNullable instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of UserAumPercentileObjectNullable given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of UserAumPercentileObjectNullable
  * @throws IOException if the JSON string is invalid with respect to UserAumPercentileObjectNullable
  */
  public static UserAumPercentileObjectNullable fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, UserAumPercentileObjectNullable.class);
  }

 /**
  * Convert an instance of UserAumPercentileObjectNullable to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

