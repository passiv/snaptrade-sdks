/*
 * SnapTrade
 *
 * Connect brokerage accounts to your app for live positions and trading
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: api@snaptrade.com
 * Generated by: https://konfigthis.com
 */

using System;
using System.Globalization;
using Newtonsoft.Json;

namespace SnapTrade.Net.Client
{
    /// <summary>
    /// Converts OpenAPI decimal-string fields between C# decimal values and JSON strings.
    /// </summary>
    public class DecimalStringJsonConverter : JsonConverter
    {
        /// <inheritdoc />
        public override bool CanConvert(Type objectType)
        {
            Type underlyingType = Nullable.GetUnderlyingType(objectType) ?? objectType;
            return underlyingType == typeof(decimal);
        }

        /// <inheritdoc />
        public override void WriteJson(JsonWriter writer, object value, JsonSerializer serializer)
        {
            if (value == null)
            {
                writer.WriteNull();
                return;
            }

            writer.WriteValue(((decimal)value).ToString(CultureInfo.InvariantCulture));
        }

        /// <inheritdoc />
        public override object ReadJson(JsonReader reader, Type objectType, object existingValue, JsonSerializer serializer)
        {
            if (reader.TokenType == JsonToken.Null)
            {
                return null;
            }

            if (reader.TokenType == JsonToken.String)
            {
                string value = (string)reader.Value;
                if (string.IsNullOrEmpty(value))
                {
                    return null;
                }

                return decimal.Parse(value, NumberStyles.Float, CultureInfo.InvariantCulture);
            }

            if (reader.TokenType == JsonToken.Integer || reader.TokenType == JsonToken.Float)
            {
                return Convert.ToDecimal(reader.Value, CultureInfo.InvariantCulture);
            }

            throw new JsonSerializationException($"Unexpected token {reader.TokenType} when parsing a decimal string.");
        }
    }
}
